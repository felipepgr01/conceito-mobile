program prjMenuLateral;

uses
  System.StartUpCopy,
  FMX.Forms,
  uPrincipal_Menu in 'uPrincipal_Menu.pas' {TFormPrincipal},
  uConfig in 'uConfig.pas' {TFormConfig};

{$R *.res}

begin
  Application.Initialize;
  Application.CreateForm(TForm1,FormPrincipal);
  Application.CreateForm(TForm2,FormConfig);
  Application.Run;
end.
